services:
  - type: web
    name: fablab-backend
    runtime: python3
    buildCommand: pip install -r requirements.txt
    startCommand: python fablab_app.py
    envVars:
      - key: FLASK_ENV
        value: production